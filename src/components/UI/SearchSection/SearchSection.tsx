import { Container, TextField } from "@mui/material";
import React from "react";

const SearchSection = () => {
  return (
    <Container>
      <TextField />
    </Container>
  );
};

export default SearchSection;
